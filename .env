
PORT=8000

# Autodesk Platform Services (optional)
APS_CLIENT_ID=
APS_CLIENT_SECRET=
APS_BUCKET=mhc-aec-bucket
APS_REGION=US

---

## How to run it now

1) Put these files into **ProjectAnalysis** exactly as shown.  
2) In a terminal at `ProjectAnalysis`:

```powershell
py -3 -m venv .venv
.\.venv\Scripts\activate
pip install -r requirements.txt
Copy-Item .env.example .env   # then open .env and paste your keys
uvicorn app.main:app --reload --port 8000
# LLM API keys
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
GOOGLE_API_KEY=AIzaSyDyO-jzm8_vPtulnalTdDe6px1HrMA7PAY
DEEPSEEK_API_KEY=   # optional (OpenAI-compatible base_url)

# BudgetGuard (monthly caps in USD)
BG_CAP_OPENAI=200
BG_CAP_ANTHROPIC=150
BG_CAP_GOOGLE=100
BG_CAP_DEEPSEEK=50

# Alerts (either Slack webhook OR SMTP if you want emails)
SLACK_WEBHOOK_URL=
EMAIL_HOST=
EMAIL_PORT=587
EMAIL_USER=
EMAIL_PASS=
EMAIL_FROM=alerts@example.com
EMAIL_TO=you@example.com
# Postgres
DATABASE_URL=postgresql+psycopg://postgres:<yourpass>@localhost:5432/projectanalysis

# Embeddings
OPENAI_API_KEY=          # if you want OpenAI embeddings
OPENAI_EMBED_MODEL=text-embedding-3-small
EMBED_DIM=1536           # must match your model (1536 for text-embedding-3-*)
# --- Postgres ---
DATABASE_URL=postgresql+psycopg://postgres:<YOUR_PASSWORD>@localhost:5432/projectanalysis

# Optional, but useful if you’ll use pgvector
PG_EMBED_DIM=1536
PG_RAG_TABLE=rag_chunks

# Server